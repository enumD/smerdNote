"devDependencies": {
"@types/node": "^24.10.1", // Typescript's type
"nodemon": "^3.1.11", // Run ts withput compiling (for production use build & start)
"ts-node": "^10.9.2", // Build and run js
"typescript": "^5.9.3" // Typescript
},
"dependencies": {
"dotenv": "^17.2.3" // Manage environment variable file
}

/ -> Homepage
